---
phase: 02-safety-gates
plan: 03
type: execute
wave: 1
depends_on:
  - 01-foundation-libraries-plan-02
files_modified:
  - lib/Test-VirtualizationStack.ps1
autonomous: true
requirements:
  - SAFE-04

must_haves:
  truths:
    - "Script validates WSL2 and Hyper-V feature state via WMI before all module changes"
    - "Script re-validates virtualization state after all modules complete and warns if changed"
    - "Script shows warning if WSL2 or Hyper-V detected as active (no modifications made to virtualization)"
    - "Script confirms no virtualization features detected if inactive"
    - "Script halts execution if virtualization detection encounters error"
    - "Virtualization validation logged via Write-OptLog and added to rollback manifest"
  artifacts:
    - path: "lib/Test-VirtualizationStack.ps1"
      provides: "Pre-flight and post-flight virtualization stack validation with before/after comparison"
      min_lines: 100
      exports: ["Test-VirtualizationStack", "Compare-VirtualizationState"]
  key_links:
    - from: "lib/Test-VirtualizationStack.ps1"
      to: "lib/Test-VirtStack.ps1"
      via: "WMI-based virtualization validation"
      pattern: "Test-VirtStack"
    - from: "lib/Test-VirtualizationStack.ps1"
      to: "Get-WindowsOptionalFeature"
      via: "WSL/Hyper-V feature state detection"
      pattern: "Get-WindowsOptionalFeature.*Microsoft-Windows-Subsystem-Linux"
    - from: "lib/Test-VirtualizationStack.ps1"
      to: "Get-CimInstance"
      via: "Hypervisor presence detection"
      pattern: "Get-CimInstance.*Win32_ComputerSystem.*Hypervisor"
    - from: "lib/Test-VirtualizationStack.ps1"
      to: "lib/Write-OptLog.ps1"
      via: "Virtualization check logging"
      pattern: "Write-OptLog.*Virtualization"
    - from: "lib/Test-VirtualizationStack.ps1"
      to: "$global:RollbackData"
      via: "Virtualization state in manifest"
      pattern: "RollbackData.*Virtualization"
---

<objective>
Create comprehensive virtualization stack validation library functions that provide pre-flight and post-flight checks to ensure WSL2 and Hyper-V remain functional throughout optimization execution.

**Purpose:** Virtualization stack preservation is critical - WSL2, Hyper-V, and Docker must continue working perfectly after all optimizations. These safety gates validate virtualization state before module execution, re-validate after completion, and warn if any changes occurred.

**Output:** Two library helper functions that can be called from the entry point (Phase 6) for pre-flight validation and post-flight comparison.
</objective>

<execution_context>
@/home/administrator/.claude/get-shit-done/workflows/execute-plan.md
@/home/administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-safety-gates/02-CONTEXT.md
@.planning/phases/02-safety-gates/02-RESEARCH.md
@.planning/phases/01-foundation-libraries/01-02-SUMMARY.md

# Phase 1 Test-VirtStack helper (existing)
From lib/Test-VirtStack.ps1 (returns hashtable with 8 boolean properties):
```powershell
function Test-VirtStack {
    [CmdletBinding()]
    [OutputType([hashtable])]
    param()

    # Returns ordered hashtable:
    # WSL_Enabled, Hypervisor_Present, HvHost_Running, vmms_Running,
    # WslService_Running, LxssManager_Running, VmCompute_Running, Overall_Healthy
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Test-VirtualizationStack library helper</name>
  <files>lib/Test-VirtualizationStack.ps1</files>
  <action>
Create lib/Test-VirtualizationStack.ps1 with one function following Phase 1 library patterns:

**Function: Test-VirtualizationStack**
- No parameters required
- Returns [hashtable] from Test-VirtStack (pre-flight state capture)
- Implements three-stage workflow:

**Stage 1: Call Test-VirtStack helper**
- Invoke existing Test-VirtStack from lib/Test-VirtStack.ps1
- Capture returned hashtable with 8 boolean properties
- Handle error if Test-VirtStack fails (catch exception)

**Stage 2: Display virtualization status to user**
- If Overall_Healthy is $true (WSL2 or Hyper-V active):
  - Show [WARNING] message in Red: "Virtualization stack detected: WSL2: {Active/Inactive}, Hyper-V: {Active/Inactive}"
  - Show confirmation message in Yellow: "This script will preserve virtualization features. No changes will be made to WSL2 or Hyper-V."
- If Overall_Healthy is $false (no virtualization):
  - Show [INFO] message in Cyan: "No virtualization features detected"

**Stage 3: Log and store virtualization state**
- Build summary strings:
  - $wslStatus = "Active" if WSL_Enabled is $true, else "Inactive"
  - $hvStatus = "Active" if Hypervisor_Present is $true, else "Inactive"
- Log via Write-OptLog:
  - Module: "SafetyGates"
  - Operation: "VirtualizationCheck"
  - Target: "VirtualizationStack"
  - Values: hashtable with WSL2, HyperV, HvHost, vmms, WslService, LxssManager status
  - Result: "Success"
  - Level: "INFO"
- Update $global:RollbackData.Virtualization hashtable:
  - WSL2: $wslStatus
  - HyperV: $hvStatus
  - Timestamp: ISO 8601 format
- Return Test-VirtStack hashtable (for post-flight comparison)

**Stage 4: Error handling**
- If Test-VirtStack call fails:
  - Show [ERROR] message in Red: "Failed to validate virtualization stack: {error details}"
  - Log error via Write-OptLog with Result="Error", Level="ERROR"
  - Exit with code 1 (halt execution - virtualization validation is critical)

**Critical requirements from 02-CONTEXT.md:**
- Check timing: Before ANY modules run (safety gate) AND after all modules complete
- If WSL2 or Hyper-V detected as active: Show warning (red color) and continue execution
- Display per-feature status: "WSL2: Active, Hyper-V: Active" (or similar)
- If NO virtualization detected: Show confirmation message "No virtualization features detected"
- Detection method: WMI (Get-WmiObject Win32_ComputerSystem) + check all related services
- Post-execution check: Compare against pre-execution status and warn if changed
- Log virtualization detection results to JSONL log file
- Add "Virtualization" field to rollback manifest with WSL2/Hyper-V status
- If detection encounters error (can't determine state): Fatal error and halt execution

**Follow Phase 1 patterns:**
- #Requires -Version 5.1 at file start
- Full comment-based help (.SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE, .NOTES)
- [CmdletBinding()] and [OutputType([hashtable])] attribute
- Try/catch error handling with Write-Error
- #region/#endregion blocks for organization
- No backtick line continuation
- Returns hashtable with structured data
- Uses $global:RollbackData for manifest updates
- Leverages existing Test-VirtStack helper (don't reimplement WMI logic)
</action>
  <verify>
grep -E "Test-VirtualizationStack" lib/Test-VirtualizationStack.ps1 | wc -l | grep -q "1"
</verify>
  <done>
Test-VirtualizationStack function exists and returns hashtable from Test-VirtStack
Function calls existing Test-VirtStack helper from lib/Test-VirtStack.ps1
Function displays warning in Red if WSL2 or Hyper-V detected as active
Function displays confirmation in Cyan if no virtualization detected
Function shows per-feature status (WSL2: Active/Inactive, Hyper-V: Active/Inactive)
Function logs virtualization detection results via Write-OptLog
Function updates $global:RollbackData.Virtualization with WSL2, HyperV, Timestamp
Function halts execution with error if Test-VirtStack fails
Function includes full comment-based help and follows Phase 1 library patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement Compare-VirtualizationState library helper</name>
  <files>lib/Test-VirtualizationStack.ps1</files>
  <action>
Add Compare-VirtualizationState function to lib/Test-VirtualizationStack.ps1 following Phase 1 library patterns:

**Function: Compare-VirtualizationState**
- Parameters:
  - [hashtable]$InitialState (required, from pre-flight Test-VirtualizationStack call)
  - [hashtable]$FinalState (required, from post-flight Test-VirtStack call)
- Returns [void] (outputs messages to console and log, no return value)
- Implements comparison workflow:

**Stage 1: Compare WSL2 state**
- Check if InitialState.WSL_Enabled differs from FinalState.WSL_Enabled
- If changed:
  - Build change message: "WSL2 changed from {Active/Inactive} to {Active/Inactive}"
  - Add to $changes array

**Stage 2: Compare Hyper-V state**
- Check if InitialState.Hypervisor_Present differs from FinalState.Hypervisor_Present
- If changed:
  - Build change message: "Hyper-V changed from {Active/Inactive} to {Active/Inactive}"
  - Add to $changes array

**Stage 3: Display comparison results**
- If $changes array has entries (state changed):
  - Show [WARNING] header in Yellow: "Virtualization state changed during execution:"
  - For each change in $changes:
    - Show bullet point in Yellow: "  - {change message}"
  - Log warning via Write-OptLog:
    - Module: "SafetyGates"
    - Operation: "VirtualizationCompare"
    - Target: "VirtualizationStack"
    - Values: hashtable with Changes (joined by '; ')
    - Result: "Warning"
    - Level: "WARNING"
- If $changes array is empty (no changes):
  - Show [SUCCESS] message in Green: "Virtualization state unchanged"

**Critical requirements from 02-CONTEXT.md:**
- Post-execution check: Compare against pre-execution status and warn if changed
- Display specific changes (WSL2 changed from X to Y, Hyper-V changed from X to Y)
- Log changes via Write-OptLog for audit trail

**Follow Phase 1 patterns:**
- Full comment-based help (.SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE, .NOTES)
- [CmdletBinding()] attribute
- Parameter validation with ValidateNotNullOrEmpty on both parameters
- #region/#endregion blocks for organization
- No backtick line continuation
- Logs via Write-OptLog
</action>
  <verify>
grep -E "Compare-VirtualizationState" lib/Test-VirtualizationStack.ps1 | wc -l | grep -q "1"
</verify>
  <done>
Compare-VirtualizationState function exists and accepts InitialState and FinalState parameters
Function compares WSL_Enabled property between InitialState and FinalState
Function compares Hypervisor_Present property between InitialState and FinalState
Function displays warning messages if virtualization state changed
Function displays success message if virtualization state unchanged
Function logs comparison results via Write-OptLog
Function includes full comment-based help and follows Phase 1 library patterns
  </done>
</task>

</tasks>

<verification>
Overall Phase 2 Plan 03 verification:

1. **Pre-flight Validation**: Run Test-VirtualizationStack on system with WSL2/Hyper-V active, verify warning appears
2. **No Virtualization**: Run Test-VirtualizationStack on system without virtualization, verify confirmation appears
3. **State Capture**: Verify function returns Test-VirtStack hashtable with all 8 boolean properties
4. **Post-flight Comparison**: Run Compare-VirtualizationState with different InitialState and FinalState, verify changes detected
5. **No Changes**: Run Compare-VirtualizationState with identical InitialState and FinalState, verify success message
6. **Logging**: Verify JSONL log file contains virtualization check and comparison entries
7. **Rollback Manifest**: Verify $global:RollbackData.Virtualization contains WSL2, HyperV, Timestamp
8. **Code Quality**: Verify no backtick line continuation, functions have comment-based help, proper error handling

Acceptance test:
```powershell
# Import functions
. .\lib\Test-VirtualizationStack.ps1

# Initialize session state (required by functions)
$global:RollbackData = @{}

# Pre-flight check
$preState = Test-VirtualizationStack
Write-Host "Pre-flight WSL: $($preState.WSL_Enabled), Hyper-V: $($preState.Hypervisor_Present)"

# Simulate post-flight check (same state - no changes)
$postState = Test-VirtStack  # Would be called again after modules
Compare-VirtualizationState -InitialState $preState -FinalState $postState
```
</verification>

<success_criteria>
Plan 03 is complete when:

1. lib/Test-VirtualizationStack.ps1 exists with Test-VirtualizationStack and Compare-VirtualizationState functions
2. Both functions follow Phase 1 library patterns (comment-based help, CmdletBinding, parameter validation)
3. Test-VirtualizationStack calls existing Test-VirtStack helper from lib/Test-VirtStack.ps1
4. Test-VirtualizationStack displays warning in Red if WSL2 or Hyper-V detected as active
5. Test-VirtualizationStack displays confirmation in Cyan if no virtualization detected
6. Test-VirtualizationStack shows per-feature status (WSL2: Active/Inactive, Hyper-V: Active/Inactive)
7. Test-VirtualizationStack logs detection results via Write-OptLog to JSONL log file
8. Test-VirtualizationStack updates $global:RollbackData.Virtualization hashtable
9. Test-VirtualizationStack halts execution with error if Test-VirtStack call fails
10. Compare-VirtualizationState compares WSL_Enabled and Hypervisor_Present between states
11. Compare-VirtualizationState displays warning if virtualization state changed
12. Compare-VirtualizationState displays success if virtualization state unchanged
13. Compare-VirtualizationState logs comparison results via Write-OptLog
14. Zero backtick line continuation in any function
15. All functions have proper error handling with try/catch blocks
</success_criteria>

<output>
After completion, create `.planning/phases/02-safety-gates/02-03-SUMMARY.md` with:
- Performance metrics (duration, tasks, files)
- Requirements completed (SAFE-04)
- Key decisions made
- Files created/modified
- Deviations from plan (if any)
- Issues encountered
- Next phase readiness (all Phase 2 safety gates complete, ready for Phase 3)
</output>
