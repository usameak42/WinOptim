---
phase: 01-foundation-libraries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/
  - modules/
  - config/
  - tests/
  - .github/ISSUE_TEMPLATE/
  - WinOptimizer.ps1
  - README.md
  - CONTRIBUTING.md
  - CHANGELOG.md
  - LICENSE
autonomous: true
requirements:
  - REPO-01
  - REPO-03

must_haves:
  truths:
    - "All directories from PRD Section 3.1 exist in repository root"
    - "config/services.json contains complete Disabled, Manual, OEM, and Protected service lists"
    - "OEM entries include detectionPattern metadata for all 4 vendors (ASUS, Lenovo, Dell, HP)"
    - "Protected services blocklist is present and prevents virtualization stack breakage"
    - "Each directory contains appropriate placeholder .gitkeep or .ps1 files"
  artifacts:
    - path: "lib/"
      provides: "Library helper directory"
      contains: "5 .ps1 files (placeholders)"
    - path: "modules/"
      provides: "Optimization module directory"
      contains: "7 .ps1 files (placeholders)"
    - path: "config/services.json"
      provides: "Service configuration with Disabled, Manual, OEM, Protected lists"
      min_lines: 100
    - path: "tests/"
      provides: "Test directory for Pester tests"
      contains: "Test-Modules.ps1, Test-Rollback.ps1 (placeholders)"
    - path: ".github/ISSUE_TEMPLATE/"
      provides: "GitHub issue templates"
      contains: "bug_report.md, feature_request.md (placeholders)"
  key_links:
    - from: "modules/*.ps1"
      to: "lib/*.ps1"
      via: "dot-sourcing in entry point"
      pattern: "\. .*lib/.*\.ps1"
    - from: "config/services.json"
      to: "Invoke-ServiceOptimize.ps1 (Phase 3)"
      via: "JSON parsing in service module"
      pattern: "Get-Content.*services.json|ConvertFrom-Json"
---

<objective>
Establish the complete repository structure and create the comprehensive services configuration file that all optimization modules will depend on. This foundational infrastructure enables the library helpers (Plan 02) to be written against a known configuration schema and directory layout.

Purpose: Create the skeleton that all subsequent phases build upon, ensuring the repository matches PRD specifications exactly and the service configuration is complete with OEM detection patterns.

Output: Complete directory structure with placeholder files, fully populated config/services.json with all vendor services and metadata.
</objective>

<execution_context>
@/home/administrator/.claude/get-shit-done/workflows/execute-plan.md
@/home/administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-libraries/01-RESEARCH.md

# Repository Structure Specification (from PRD Section 3.1)

```
WinOptimizer/
├── WinOptimizer.ps1           # Main entry point (Phase 6)
├── lib/                       # Shared library helpers (Phase 1)
│   ├── Write-OptLog.ps1
│   ├── Get-ActivePlanGuid.ps1
│   ├── Save-RollbackEntry.ps1
│   ├── Take-RegistryOwnership.ps1
│   └── Test-VirtStack.ps1
├── modules/                   # Optimization modules (Phases 3-5)
│   ├── Invoke-TelemetryBlock.ps1
│   ├── Invoke-GpuDwmOptimize.ps1
│   ├── Invoke-SchedulerOptimize.ps1
│   ├── Invoke-PowerPlanConfig.ps1
│   ├── Invoke-FileSystemOptimize.ps1
│   ├── Invoke-ServiceOptimize.ps1
│   └── Invoke-Rollback.ps1
├── config/                    # Configuration files (Phase 1)
│   └── services.json
├── tests/                     # Pester tests (Phase 7)
│   ├── Test-Modules.ps1
│   └── Test-Rollback.ps1
├── README.md                  # Documentation (Phase 7)
├── CONTRIBUTING.md            # Contribution guidelines (Phase 7)
├── CHANGELOG.md               # Version history (Phase 7)
├── LICENSE                    # MIT license (Phase 7)
└── .github/                   # GitHub templates (Phase 7)
    └── ISSUES_TEMPLATE/
        ├── bug_report.md
        └── feature_request.md
```
</context>

<tasks>

<task type="auto">
  <name>Create complete repository directory structure</name>
  <files>lib/, modules/, config/, tests/, .github/ISSUE_TEMPLATE/</files>
  <action>
Create all directories specified in PRD Section 3.1:

1. Create lib/ directory (will contain 5 helper scripts in Plan 02)
2. Create modules/ directory (will contain 7 optimization modules in later phases)
3. Create config/ directory (will contain services.json in this plan)
4. Create tests/ directory (will contain Pester tests in Phase 7)
5. Create .github/ISSUE_TEMPLATE/ directory (will contain templates in Phase 7)

Use `mkdir -p` to create nested directories in one command. No placeholder files needed yet (config file is created in next task).

Do NOT create WinOptimizer.ps1, README.md, CONTRIBUTING.md, CHANGELOG.md, or LICENSE files yet - those are created in later phases (6 and 7).
  </action>
  <verify>ls -la /mnt/d/Coding/WinOptim/ shows lib/, modules/, config/, tests/, .github/ directories exist</verify>
  <done>All 5 required directories exist in repository root</done>
</task>

<task type="auto">
  <name>Create config/services.json with complete service lists</name>
  <files>config/services.json</files>
  <action>
Create config/services.json with the following structure (based on RESEARCH.md Example 2):

```json
{
  "disabled": [
    {"name": "DiagTrack", "reason": "Microsoft telemetry data collection service"},
    {"name": "dmwappushservice", "reason": "Windows Push Messaging User Data Service (telemetry)"},
    {"name": "MapsBroker", "reason": "Downloaded Maps Manager - unused for development workstations"},
    {"name": "RetailDemo", "reason": "Retail Demo mode service - not needed"},
    {"name": "WerSvc", "reason": "Windows Error Reporting - can be disabled"},
    {"name": "wisvc", "reason": "Windows Insider Service - not needed"},
    {"name": "NvTelemetryContainer", "reason": "Nvidia telemetry container service"}
  ],
  "manual": [
    {"name": "SysMain", "reason": "Superfetch/Prefetch - conditionally disabled in File System module for NVMe"},
    {"name": "WSearch", "reason": "Windows Search - configured with exclusions, not fully disabled"},
    {"name": "lfsvc", "reason": "Geolocation Service - rarely needed"},
    {"name": "PeerDistSvc", "reason": "BranchCache - not needed for single-user workstation"},
    {"name": "SharedAccess", "reason": "Internet Connection Sharing - rarely needed"},
    {"name": "PrintNotify", "reason": "Print spooler notification - not needed if no printer"},
    {"name": "icssvc", "reason": "Windows Mobile Hotspot - rarely needed"},
    {"name": "NcdAutoSetup", "reason": "Network Connection Auto-Setup - not needed"},
    {"name": "PhoneSvc", "reason": "Phone Service - not needed"},
    {"name": "RmSvc", "reason": "Radio Management - rarely needed"}
  ],
  "oem": {
    "asus": [
      {
        "name": "ArmouryCrate.Service",
        "displayName": "ASUS Armoury Crate Service",
        "detectionPattern": {"type": "Service", "query": "Get-Service ArmouryCrate.Service -ErrorAction SilentlyContinue"},
        "countermeasure": "PowerPlanReassertion",
        "description": "Reasserts ASUS power plan at boot; requires scheduled task countermeasure"
      },
      {
        "name": "ASUSOptimization",
        "displayName": "ASUS Optimization Service",
        "detectionPattern": {"type": "Service", "query": "Get-Service ASUSOptimization -ErrorAction SilentlyContinue"},
        "countermeasure": "PowerPlanReassertion",
        "description": "ASUS power management override service"
      }
    ],
    "lenovo": [
      {
        "name": "LenovoVantage",
        "displayName": "Lenovo Vantage Service",
        "detectionPattern": {"type": "Service", "query": "Get-Service LenovoVantage -ErrorAction SilentlyContinue"},
        "countermeasure": "PowerPlanReassertion",
        "description": "Lenovo power management override service"
      }
    ],
    "dell": [
      {
        "name": "DellCommandCenter",
        "displayName": "Dell Command Center",
        "detectionPattern": {"type": "Service", "query": "Get-Service DellCommandCenter -ErrorAction SilentlyContinue"},
        "countermeasure": "PowerPlanReassertion",
        "description": "Dell power management override service"
      }
    ],
    "hp": [
      {
        "name": "HP Omen Gaming Hub",
        "displayName": "HP Omen Gaming Hub Service",
        "detectionPattern": {"type": "Service", "query": "Get-Service 'HP Omen Gaming Hub' -ErrorAction SilentlyContinue"},
        "countermeasure": "PowerPlanReassertion",
        "description": "HP power management override service"
      }
    ]
  },
  "protected": [
    {"name": "HvHost", "reason": "Hyper-V Host Service - CRITICAL for virtualization"},
    {"name": "vmms", "reason": "Hyper-V Virtual Machine Management - CRITICAL for virtualization"},
    {"name": "WslService", "reason": "WSL Service - CRITICAL for WSL2"},
    {"name": "LxssManager", "reason": "WSL Lxss Manager - CRITICAL for WSL2"},
    {"name": "VmCompute", "reason": "Hyper-V Compute Service - CRITICAL for virtualization"},
    {"name": "vmic*", "reason": "All Hyper-V VM Integration Services - CRITICAL for VM functionality"}
  ],
  "metadata": {
    "version": "1.0.0",
    "lastUpdated": "2026-03-13",
    "description": "Windows service configuration for WinOptimizer - Disabled, Manual, OEM, and Protected service lists",
    "extensibility": "Users can add custom entries to disabled/manual lists. Protected list MUST NOT be modified."
  }
}
```

This JSON structure matches the user decision from CONTEXT.md requiring rich metadata with nested categories and OEM detection patterns.
  </action>
  <verify>Test-Path /mnt/d/Coding/WinOptim/config/services.json returns true; Get-Content /mnt/d/Coding/WinOptim/config/services.json | ConvertFrom-Json returns valid object with disabled, manual, oem, protected, metadata properties</verify>
  <done>config/services.json exists with all 7 disabled services, 10 manual services, 4 OEM vendor entries, 6 protected services, and metadata</done>
</task>

<task type="auto">
  <name>Create placeholder files for lib/, modules/, and tests/ directories</name>
  <files>lib/*.ps1, modules/*.ps1, tests/*.ps1</files>
  <action>
Create placeholder files for each directory:

**lib/ directory** (5 placeholder files):
- lib/Write-OptLog.ps1
- lib/Get-ActivePlanGuid.ps1
- lib/Save-RollbackEntry.ps1
- lib/Take-RegistryOwnership.ps1
- lib/Test-VirtStack.ps1

Each placeholder should contain:
```powershell
#Requires -Version 5.1
# Placeholder for LIBR-XX function
# Implemented in Plan 02
```

**modules/ directory** (7 placeholder files):
- modules/Invoke-TelemetryBlock.ps1
- modules/Invoke-GpuDwmOptimize.ps1
- modules/Invoke-SchedulerOptimize.ps1
- modules/Invoke-PowerPlanConfig.ps1
- modules/Invoke-FileSystemOptimize.ps1
- modules/Invoke-ServiceOptimize.ps1
- modules/Invoke-Rollback.ps1

Each placeholder should contain:
```powershell
#Requires -Version 5.1
# Placeholder for optimization module
# Implemented in later phases
```

**tests/ directory** (2 placeholder files):
- tests/Test-Modules.ps1
- tests/Test-Rollback.ps1

Each placeholder should contain:
```powershell
# Requires Pester
# Placeholder for Pester tests
# Implemented in Phase 7
```

These placeholders ensure the directory structure is complete and ready for implementation in subsequent phases.
  </action>
  <verify>Test-Path /mnt/d/Coding/WinOptim/lib/Write-OptLog.ps1 returns true; same for all 5 lib files, 7 module files, 2 test files</verify>
  <done>All 14 placeholder files exist (5 lib helpers, 7 modules, 2 tests)</done>
</task>

</tasks>

<verification>
After completion, verify:

1. Directory structure matches PRD Section 3.1 exactly:
   ```bash
   tree -L 2 /mnt/d/Coding/WinOptim/
   ```

2. config/services.json is valid JSON with all required sections:
   ```powershell
   $services = Get-Content /mnt/d/Coding/WinOptim/config/services.json | ConvertFrom-Json
   $services.disabled.Count -eq 7
   $services.manual.Count -eq 10
   $services.oem.asus.Count -eq 2
   $services.oem.lenovo.Count -eq 1
   $services.oem.dell.Count -eq 1
   $services.oem.hp.Count -eq 1
   $services.protected.Count -eq 6
   ```

3. All placeholder files exist and contain #Requires -Version 5.1:
   ```bash
   grep -r "#Requires -Version 5.1" /mnt/d/Coding/WinOptim/lib/ /mnt/d/Coding/WinOptim/modules/ /mnt/d/Coding/WinOptim/tests/
   ```
</verification>

<success_criteria>
Phase 1 Plan 01 is complete when:

1. All 5 directories exist (lib/, modules/, config/, tests/, .github/ISSUE_TEMPLATE/)
2. config/services.json contains complete service lists with rich metadata
3. OEM entries include detection patterns for all 4 vendors
4. Protected services blocklist prevents virtualization stack breakage
5. All 14 placeholder files exist with #Requires -Version 5.1
6. Repository structure is ready for Plan 02 (library helper implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-libraries/01-01-SUMMARY.md` documenting:
- Directories created with timestamps
- config/services.json structure and service counts
- Placeholder files created
- Any deviations from PRD structure (should be none)
- Verification results from JSON parsing and directory listing
</output>
